const ARTICLE_STATUSES = Object.freeze(['DRAFT', 'REVIEW', 'PUBLISHED', 'ARCHIVED']);

const CATEGORY_TYPES = Object.freeze([
  'IT',
  'HR',
  'FINANCE',
  'PROCUREMENT',
  'FACILITIES',
  'ADMINISTRATION',
]);

const FEEDBACK_TYPES = Object.freeze(['GENERAL', 'HELPFUL', 'NOT_HELPFUL', 'SUGGESTION']);

const ARTICLE_ADMIN_ROLES = Object.freeze(['ADMIN', 'SUPER_ADMIN']);

const ARTICLE_PUBLISH_ROLES = Object.freeze(['MANAGER', 'ADMIN', 'SUPER_ADMIN']);

const ARTICLE_AUTHOR_ROLES = Object.freeze(['EMPLOYEE', 'MANAGER', 'HR', 'ADMIN', 'SUPER_ADMIN']);

const ANALYTICS_ROLES = Object.freeze(['MANAGER', 'HR', 'ADMIN', 'SUPER_ADMIN']);

module.exports = {
  ARTICLE_STATUSES,
  CATEGORY_TYPES,
  FEEDBACK_TYPES,
  ARTICLE_ADMIN_ROLES,
  ARTICLE_PUBLISH_ROLES,
  ARTICLE_AUTHOR_ROLES,
  ANALYTICS_ROLES,
};
